from fastapi import APIRouter, HTTPException, Response, Depends
from app.schemas.


router = APIRouter()


@router.post("/register-user-email-verification")
def register_user_email_verification(data:)
